class Shape {
	constructor(x, y, color, pWidth) {
		this.x = x;
		this.y = y;
		this.color = color;
		this.pWidth = pWidth;

	}
	printValues(){
		console.log("x:" + this.x + "y:" + this.y + "color:" + this.color);

	}
}